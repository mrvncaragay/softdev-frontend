import { makeStyles } from '@material-ui/styles';

export default makeStyles(theme => ({
  root: {
    display: 'flex'
  },

  classicLogin: {
    flex: 1
  },

  socialMediaLogin: {
    flex: 1
  },

  btnSubmit: {
    marginTop: theme.spacing(2)
  }
}));
