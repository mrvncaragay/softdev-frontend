// Palette
import palette from '../palette';

export default {
  label: {
    '&:hover': {
      color: palette.background.default,

      '& .activeLink': {
        color: palette.background.default
      }
    }
  }
};
